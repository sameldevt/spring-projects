package br.com.med.voll.api.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import med.voll.api.entities.medico.Medico;

// Repositories são interfaces que simplificam o conceito DAO (data access object), muito usado
// em aplicações que nao usam o spring 
public interface MedicoRepository extends JpaRepository<Medico, Long> {

}
